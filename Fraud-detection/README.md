# Fraud Detection ML Pipeline

### Before going any further
Make sure to download dataset first from the *download_source.txt* file in the `dataset` folder.
The dataset contains approx 6362620 rows and 10 columns and has size of approx. 500+ MiB, therefore I can't upload it to github.com

## Setup
```bash
pip install -r requirements.txt
```
